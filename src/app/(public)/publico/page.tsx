import { PublicPortalClient } from "@/features/public/components/public-portal-client";

export const metadata = {
  title: "Portal público",
};

export default function PublicoPage() {
  return <PublicPortalClient />;
}
